/* Build Well — brand tokens
   Source of truth: DESIGN.buildwell.md (extracted from buildwell_logo.png)
   Import into every composition. */

:root {
  /* Canvas */
  --bw-green: #366560;        /* primary background (logo green) */
  --bw-green-deep: #294f4b;   /* cards, panels, shadows */
  --bw-green-soft: #51716d;   /* borders, dividers, hairlines */

  /* Accent */
  --bw-blush: #edb8b6;        /* primary accent (logo blush) */
  --bw-blush-deep: #d99a97;   /* accent variant, small-text contrast */

  /* Light canvas + text */
  --bw-ivory: #f7f1ea;        /* light background + text on green */
  --bw-ink: #22302e;          /* text on ivory */
  --bw-text-dim: #b9cdc9;     /* secondary text on green */
  --bw-ink-dim: #5f6f6c;      /* secondary text on ivory */

  /* Caption pill background */
  --bw-caption-bg: rgba(41, 79, 75, 0.85);

  /* Typography */
  --font-display: "Fraunces", Georgia, serif;      /* headlines, quotes, numbers */
  --font-label: "Montserrat", system-ui, sans-serif; /* kickers, labels, captions, CTAs */
  --label-tracking: 0.12em;
}
